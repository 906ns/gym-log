# 2026-09 レビュー・プロンプト2対応

作業ブランチは `codex/fix-phase1`。レビュー文書はこのブランチに存在しないため、前回指定された `origin/claude/funny-ptolemy-ccty7b:docs/review-2026-09.md` の共通ブロック・プロンプト2を参照した。mainの変更・プッシュは行っていない。

## 対応表

| 指摘 | 変更ファイル | 対応・判断 |
| --- | --- | --- |
| 2-1 | js/views/session.js | 空状態では「種目を選ぶ」だけを表示し、追加ボタンを非表示 |
| 2-2 | 同上 | 未記録なら最大0kgの代わりに「未記録」 |
| 2-3 | 同上 | 前回の再追撃で対応済み。today.length条件を維持し、今回も画面で確認 |
| 2-4 | 同上、css/style.css、README.md | 種目名で開閉、別ボタン「履歴」で履歴を開く |
| 2-5 | index.html、css/style.css、README.md | closeを×に変更。開閉には既存forwardを回転して使用。symbolは15個のまま |
| 2-6 | js/views/summary.js、css/style.css | 取っ手を先頭に維持し、その後にアイコンを配置。余白と表示寸法を縮小 |
| 2-7 | css/style.css | シートのsticky見出し背景をsurfaceに統一 |
| 2-8 | 同上 | 44×44の操作領域内に小さなチェック枠を描画。チェック時は真鍮色とチェック記号 |
| 2-9 | js/views/settings.js | 設定セクションの固定件数を削除。種目件数は維持 |
| 2-10 | 同上 | 「種目一覧」の見出しと追加ボタンを部位グループの前に配置 |
| 2-11 | js/views/history.js | 進行中なら開始ではなく「トレーニングを続ける」を表示 |
| 2-12 | js/lib/calc.js、js/views/session.js、test/calc.js.test.js | 前回比を符号付き表示。丸め前の符号を保持し微小な減少も-0.0と表示 |
| 2-13 | js/lib/calendar.js、js/views/home.js、test/calendar.test.js | カレンダーを週次集計と同じ月曜始まりに統一 |
| 2-14 | js/lib/input.js、js/views/ui.js、test/input.test.js | 空欄をnullとして扱い、レップは1から、重量は既存の刻み幅から増加 |
| 2-15 | js/lib/input.js、js/views/session.js、js/views/weight-control.js、css/style.css、test/input.test.js | 重量・レップの不備をまとめて入力パネル内に表示。未入力、数字以外、範囲外を区別。aria-invalidと説明参照も付与 |
| 2-16 | js/views/settings.js | グローバル休憩が新規種目の初期値であることと既存種目の変更場所を明記。保存挙動は変更なし |
| 2-17 | js/views/session.js | 刻みシートに、この種目へ保存され次回も使うことを明記 |
| 2-18 | css/style.css | 部位チップを最小44×44に。刻みチップの高さも44へ統一 |
| 2-19 | js/views/exercise-history.js | テキストの閉じるボタンを本文末尾へ移動。上部取っ手の閉じる操作も維持 |
| 共通 | sw.js、js/views/settings.js | CACHEと表示をgym-log-glass-7に更新。追加App Shellファイルなし |

AGENTS.md、package.json、DBスキーマ、記録の保存形式、シードIDは変更していない。プロンプト3・4は対象外。

## 自動確認

- 各コミット前にnpm testを実行。最終結果は32件成功、失敗0（開始時28件から4件追加）。
- 全JSをnode --checkで構文確認: 成功。
- `grep -rn "innerHTML" js/`: 出力なし。
- `grep -rn 'href="/\|src="/' index.html js/ css/`: 出力なし。
- `git diff --check`: 成功。
- CACHE表示一致、SHELL記載ファイル存在、SVG symbol数15: コマンドで確認。

## 画面確認

独立したローカルオリジンのサブパス `/gym-log/` を静的配信し、Chromiumで操作した。390×664のダークとライトを設定画面から切り替えてスクリーンショットを目視確認した。

- 両テーマ: 設定の件数、種目一覧見出し、部位グループの外にある追加ボタンを確認。チェックONは真鍮色とチェック記号、OFFは無彩色の枠。操作領域は44×44。
- 両テーマ: セッション空状態の開始操作が1個。種目名の開閉、未記録表示、履歴が別操作であることを確認。
- 両テーマ: ピッカー見出しとシート背景が一致（ライトrgb(255,255,255)、ダークrgb(31,36,39)）。「胸・肩・脚」は44×44。
- 両テーマ: 終了サマリーの取っ手Y=199.60〜243.60、アイコンY=243.60〜275.60で重なり0。ホームへ戻るボタンY=592〜648で画面内。scrollHeight=clientHeight=464、scrollTop=0。
- ライト: 進行中かつ終了履歴なしで、履歴画面が「トレーニングを続ける」になり、「開始する」が出ないことを確認。
- ライト: 重量・レップ両方未入力のまま記録すると、両方のエラーが入力直下に表示。レップの＋1回で1になり、重量60を入れると保存成功。
- ダーク: 60kgの前回に対し70kgを記録して「前回比 +10.0 kg」を確認。
- 刻みの保存説明、休憩初期値の説明、履歴本文末尾の閉じる操作を確認。
- 月曜始まりの曜日列をアクセシビリティツリーで確認。月初の列・閏月は単体テストで確認。

## 前回修正の回帰確認（390×568）

前回3セット・今日0セットの検証データを用意。ダークで種目を開き、座標クリックで3セット連続記録した。

- 前回3行: 175.5〜223.5、223.5〜271.5、271.5〜319.5。開始時と3セット後とも同じ位置で完全表示。
- 今日0セット: 重量449〜497、記録504〜560。
- 今日1〜3セット: 重量399〜447、記録454〜510。手動スクロールなし。
- 一覧scrollTopは8で維持。入力・休憩は引き続き独立したフレックス領域。

## 未確認

- iPhone Safari実機、ソフトウェアキーボード、日本語IME、safe-areaの実寸。
- standalone起動、更新SWへの切り替え、実機オフライン動作。
- 高倍率の文字サイズ変更や非常に長い種目名・メモでの全組み合わせ。
- 履歴の空状態と入力エラーはライトで操作確認、ダークの同じ状態は個別の操作検証を省略。レイアウトと色の共通部分は両テーマで確認。
