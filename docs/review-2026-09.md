# gym-log レビュー結果と修正依頼プロンプト（2026-09）

v1.2（コミット da1f73d）時点のコードレビューと、iPhone 13 相当の viewport（390x664）での
実動作確認をもとにした修正依頼。指摘内容・実測値・受け入れ条件をそのままプロンプトにしてある。

段階1〜3を **必ず 1 → 2 → 3 の順**で実施し、各段階ごとにコミットして動作確認してから次へ進むこと。
段階4はパフォーマンス改善で、記録データが増えてからで構わない。
各プロンプトの先頭に「共通ブロック」を貼り付けて使う。

対応が完了したら、このファイルは削除してよい。

---

## 共通ブロック（各プロンプトの前に必ず貼る）

```
あなたはリポジトリ 906ns/gym-log（個人用の筋トレ記録 PWA）を修正します。

## 最初に読むもの
- AGENTS.md（このリポジトリの最上位ルール。必ず全文読む）
- docs/ui.md, docs/v1.2-ui.md, docs/v1.2-theme.md（画面仕様）
- README.md（起動・更新手順）

## 絶対に守る制約（AGENTS.md より。破ったらやり直し）
- ビルドツール・フレームワーク・npm 依存・CDN 読み込みを一切追加しない。
  素の HTML / CSS / JavaScript (ESM) のみ。package.json は触らない。
- 外部ネットワーク通信を追加しない。
- DOM 構築に innerHTML を使わない。createElement と textContent のみ。
- 絶対パス（href="/..." src="/..."）を書かない。相対パスのみ（GitHub Pages のサブパス配下で動く必要がある）。
- js/lib/ の中身は DOM と IndexedDB を参照しない純関数のみ（例外は wakelock.js の navigator のみ）。
- views/ から db.js を直接呼ばない。必ず repo.js 経由。
- 物理削除を実装しない。削除は deleted_at を立てる論理削除のみ。
- 読み取りは必ず deleted_at === null でフィルタする。
- すべての input の font-size は 16px 以上（iOS Safari の自動ズーム防止）。
- コメントは日本語で「なぜそうしたか」を書く。何をしているかはコードで示す。
- セミコロンあり、インデント2スペース、シングルクォート、const 優先、var 禁止。
- グローバル変数を作らない。

## 重要：AGENTS.md 自体が古い箇所がある
以下の3点は AGENTS.md と実装が矛盾しているが、**実装側が正しい**（v1.1 / v1.2 で意図的に決めた変更で、
AGENTS.md の更新漏れ）。AGENTS.md に合わせて実装を巻き戻さないこと。
- AGENTS.md §2 が localStorage を全面禁止しているが、index.html:14-34 のテーマキャッシュは仕様通り（README に記載あり）。
- AGENTS.md §4 が「css/style.css 単一ファイル」としているが、css/glass.css の存在は仕様通り。
- AGENTS.md §11 がスコープ外としている「推移グラフ」「ダークモード以外のテーマ」は v1.1/v1.2 で実装済み。
AGENTS.md の更新は段階3で行う。段階1・2では AGENTS.md を編集しない。

## 作業の進め方
- ブランチ: 現在のブランチで作業する。勝手に main へ push しない。
- 変更は最小限に。今回指示された項目以外をリファクタリングしない。
- 1項目ずつ直し、項目ごとに意味のある単位でコミットする（コミットメッセージは日本語）。
- 完了前に必ず実行して全部通すこと:
    npm test
    grep -rn "innerHTML" js/        # 出力が空であること
    grep -rn 'href="/\|src="/' index.html js/ css/   # 出力が空であること
- App Shell のファイル構成を変えた場合は sw.js の SHELL 配列に追加する。
  段階の最後に sw.js の CACHE 名を1つ上げ、js/views/settings.js の「キャッシュ」表示値も同じ値に合わせる。

## 動作確認の方法
静的配信して確認できる:
    mkdir -p /tmp/gym-log-preview/gym-log
    cp -R index.html css js icons data manifest.webmanifest sw.js /tmp/gym-log-preview/gym-log/
    python3 -m http.server 8000 --bind 127.0.0.1 --directory /tmp/gym-log-preview
    # http://localhost:8000/gym-log/ を開く
確認は必ず **iPhone 相当の狭い viewport（390x664 程度）** で行うこと。
デスクトップ幅では今回の不具合の多くが再現しない。

## 報告に含めること
1. 変更したファイルと、各変更が「どの指摘」に対応するか。
2. 直さなかった項目と、その理由。
3. 実機 iPhone Safari でしか確認できない項目のリスト。
推測で「動作します」と書かないこと。確認していない項目は「未確認」と明示する。
```

---

## プロンプト1 ── 🔴 致命的な不具合（最優先・単独で先にやる）

```
以下の4件を修正してください。1件ずつ直し、1件ずつ確認し、1件ずつコミットしてください。

────────────────────────────────────────
【1-1】種目カードで「前回のセット」と「今日のセット」が入力パネルに隠れる（最重要）

該当: css/style.css:73
  .card > .controls { position: sticky; bottom: var(--rest-offset, 0px); z-index: 2; ... }

原因:
.controls は js/views/session.js の expanded() でカードの最後の子として append されている。
カードが viewport より高いとき、position:sticky + bottom は要素を本来の位置から「上へ引き上げて」
画面下端に貼り付ける。.controls は background: var(--surface-2) の不透明ブロック + z-index:2 なので、
引き上げられた分だけ真上にあるコンテンツを完全に塗りつぶす。

実測（iPhone 13 相当 / viewport 高さ 664px / 2日目に同じ種目を開いた状態）:
  前回 60.0 kg × 10        → y 326-374
  前回 65.0 kg × 8         → y 374-422
  前回 70.0 kg × 6「メモ」  → y 422-470
  「今日」見出し            → y 470-497
  .controls（入力パネル）   → y 378-664   ← 上3つを覆い隠している
前回3セット中、見えるのは1セット目だけ。メモ付きのセットも見えない。
セットを記録した直後も、記録済みの行が1行も見えない。

なぜ重大か:
AGENTS.md §1 が「最重要機能は記録そのものではなく、種目を開いた瞬間に前回の記録が見えること」
「この動線を遅くする変更は他のどんな利点よりも優先して却下すること」と明記している。
現状はこのアプリの存在理由そのものが機能していない。

受け入れ条件:
- 種目カードを開いた直後、前回のセット行が（何セットあっても）入力パネルに隠されないこと。
- セットを記録した直後、今日記録した行が入力パネルに隠されないこと。
- viewport 390x664 で、前回3セット + 今日3セットの状態を実際に表示して確認すること。
- .rest フッター（画面下端の休憩表示）と入力パネルが重ならないこと（--rest-offset の役割を壊さない）。

推奨アプローチ（必須ではない。より良い案があればそれで良い）:
案A: .controls を expanded() 内でカードの「先頭側」（前回リストより上）に配置し、
     sticky を bottom ではなく top で貼る。
案B: .controls の sticky を外して通常フローに戻し、画面下端固定は .rest フッターだけにする。
いずれの案でも、「隠れない」ことを実際の描画で確認してから完了とすること。

────────────────────────────────────────
【1-2】ホームの主要ボタンが、どの iPhone でも初期表示で画面外

該当: js/views/home.js:48
  root.replaceChildren(overview, body, calendar, start, past);

問題:
「今日のトレーニングを開始」/「トレーニングを続ける」ボタンが、
週次概要（76px の巨大数字）+ 体重カード + カレンダー1ヶ月ぶん の下にある。

実測（記録0件のホーム画面）:
  iPhone SE        viewport 568 / ドック上端 496 / ボタン y 727-783 → 見えない
  iPhone 13        viewport 664 / ドック上端 592 / ボタン y 727-783 → 見えない
  iPhone 14 Pro Max viewport 740 / ドック上端 668 / ボタン y 727-783 → 見えない
全サイズでスクロールしないと押せない。ジムで最初に行う操作がこれ。

受け入れ条件:
- iPhone SE 相当（390x568）でホームを開いた初期表示で、開始/続けるボタンが
  スクロールなしで完全に見えること（下端の浮動ドックにも隠れないこと）。
- 「過去の日付で記録する」ボタンとの関係（past.disabled の挙動）を壊さないこと。
- 体重カード・カレンダー・週次概要が引き続き到達可能であること。

推奨アプローチ: start / past を overview の直後に移動する。

────────────────────────────────────────
【1-3】エラー表示が成功後も消えない

該当:
  js/views/ui.js:11-20  showError() が #error / #session-error を表示する
  js/app.js:40-41       これらを hidden に戻すのは navigate() だけ
  js/views/session.js:55-62  reload() はエラーをクリアしない

再現（実測ログ）:
  重量未入力で「記録する」→ エラー表示（error hidden? false）
  重量を入れて「記録する」→ 記録は成功するが error hidden? **false のまま**
  以降、画面を切り替えるまで赤いエラー枠が画面最上部に居座る。

受け入れ条件:
- 記録に成功した時点で、セッション画面のエラー表示が消えること。
- 同様に、体重記録や種目保存など他の成功操作でも、対応するエラー表示が残らないこと。
- エラーが出るべき場面では今まで通り出ること（回帰させない）。

────────────────────────────────────────
【1-4】全角数字が拒否される

該当:
  js/lib/calc.js:3    numberInput() の正規表現 /^(?:\d+(?:\.\d*)?|\.\d+)$/
  js/lib/units.js:12  parseWeightInput() の同じ正規表現

問題:
日本語キーボードから入力した「６０」が「数値を入力してください」で拒否される。
日本語 UI のアプリとして、全角数字は正常系として受け入れるべき。

受け入れ条件:
- 「６０」「１０」「６２．５」が半角と同じ結果になること。
- 不正な入力（"abc", "-5", "", "1.2.3"）は今まで通り拒否されること。
- js/lib/ は純関数のままであること（DOM/IndexedDB を参照しない）。
- test/calc.js.test.js と test/units.test.js に全角入力のテストを追加すること。
  テストは日本語で仕様を記述する（例: it('全角数字も半角と同じ値として受け付ける')）。

推奨アプローチ: 正規表現に通す前に String(value).normalize('NFKC') を適用する。

────────────────────────────────────────
最後に:
- npm test が全件パスすること。
- sw.js の CACHE 名を1つ上げ、js/views/settings.js:65 の「キャッシュ」表示値を同じ値に合わせること。
```

---

## プロンプト2 ── 🟠 UI / UX の仕上げ

```
プロンプト1の修正が完了している前提で、以下を修正してください。
表示上の変更が中心です。1項目ずつ確認し、意味のある単位でコミットしてください。
viewport 390x664 のダークテーマとライトテーマの両方で必ず目視確認すること。

【2-1】セッション空状態のボタン重複
  js/views/session.js:42 が常に「＋ 種目を追加」を表示し、
  js/views/session.js:67 が空状態でさらに「種目を選ぶ」を表示する。
  同じ動作（openPicker）のボタンが縦に2つ並ぶ。どちらか1つにする。

【2-2】未記録の種目に「0セット / 最大0.0 kg」と出る
  js/views/session.js:83。today.length === 0 のときは重量部分を出さない。

【2-3】中身が空でも「今日」見出しが出る
  js/views/session.js:113。今日のセットが0件なら見出しごと出さない。

【2-4】種目名タップの行き先が直感に反する
  js/views/session.js:76-88。大きな種目名（h2 内の .exercise-name）をタップすると
  履歴シートが開き、開閉トグルは2行目の小さい行（.card-heading）になっている。
  ユーザーは大きい名前をタップして開閉を期待する。
  どちらの操作も残したまま、開閉が主・履歴が副だと分かる配置/アフォーダンスに変える。

【2-5】「閉じる」アイコンが下向きシェブロン
  index.html:46 の symbol id="icon-close" は下向きシェブロン（∨）。
  これが「閉じる」ボタン（js/views/ui.js:24 のマッピング）にもカードの開閉トグルにも使われており、
  下向き矢印＝展開と誤読される。
  ※ AGENTS.md / docs のアイコン方針（許可された15種類の symbol 参照に統一）は維持すること。
    symbol を1つ追加してよいかは docs/v1.2-ui.md を確認し、増やす場合は index.html の defs と
    使用箇所の両方を更新する。

【2-6】終了サマリーのレイアウト崩れ
  js/views/summary.js:14 の modal.prepend(seal) が、js/views/ui.js:96 で prepend される
  .sheet-grabber より前に入る。.sheet-grabber は margin:-16px 0 0 で「最初の子」を前提にしているため、
  実測で seal(y125-169) と grabber(y153-197) が16px重なる。
  さらに「ホームへ戻る」ボタンが y669-725 で、シート下端 664 の外（scrollHeight 648 > clientHeight 563）。
  毎回スクロールしないと閉じられない。
  受け入れ条件: 取っ手とアイコンが重ならないこと / 閉じるボタンが viewport 390x664 で
  スクロールなしで押せること。

【2-7】シート内のセクション見出しの背景色が合っていない
  css/style.css:182 の .section-heading { background: var(--bg) } が dialog.sheet の中でも効き、
  シート背景（var(--surface)）より暗い帯が出る。
  実測: シート rgb(31,36,39) / 見出し rgb(22,25,27)。種目ピッカーで顕著。
  sticky 時に下のコンテンツが透けない前提は維持したまま、文脈に応じた背景色にする。

【2-8】チェックボックスが巨大な白い四角に見える
  css/style.css:62 の input[type="checkbox"] { display:inline-block; width:44px } と
  css/style.css:13 の共通ルール（min-height:44px, background, border）が組み合わさり、
  ネイティブのチェックボックスが 44x44 に引き伸ばされる。
  実測: 44x44 / background rgb(231,234,235) / appearance auto。
  ライトテーマでは大きな白い四角にしか見えず、ON/OFF が判別できない。
  受け入れ条件: タップ領域 44x44 を維持したまま、ON/OFF がひと目で分かること。
  ダーク・ライト両方で確認すること。

【2-9】設定画面のセクション件数がベタ書きかつ意味が不統一
  js/views/settings.js:59-65 で sectionHeading('表示', 2) 等の数字がハードコードされており、
  行を増減すると嘘になる。さらに「表示 2」（設定項目数）と「胸 2」（種目数）が同じ見た目で意味が違う。
  受け入れ条件: 設定セクションの件数はコードから導出するか、表示をやめる。
  種目グループの件数表示は維持してよい。

【2-10】種目一覧に見出しがなく、「種目を追加」が脚グループに紛れる
  js/views/settings.js:59-72。透明度オフ行の直後にいきなり「胸」「肩」…が始まり、
  ここが種目一覧だと分からない。また「種目を追加」行が最後の部位グループの末尾に見える。
  受け入れ条件: 種目一覧であることが分かる見出しを置き、「種目を追加」が
  特定の部位に属して見えないようにする。

【2-11】履歴タブの空状態が事実と矛盾する
  js/views/history.js:8 は recentSessions()（ended_at !== null）なので進行中セッションを含まない。
  結果、記録中でも js/views/history.js:11 の
  「最初のトレーニングを記録する / 開始する」が表示され、真下のミニバーが
  「記録中 0:06 / 1種目 1セット」と表示する（矛盾）。
  さらに「開始する」を押しても startSession() が既存セッションを返すだけで新規開始しない。
  受け入れ条件: 進行中セッションがある場合、履歴タブの空状態は「開始する」を出さないこと。

【2-12】「前回比」に符号が付かない
  js/views/session.js:89 が formatTotal() を使うため、記録更新時に
  「推定1RM 93.3 kg / 前回比 13.3 kg」と表示され、隣の絶対値と区別が付かない（実測）。
  悪化時だけ「-13.3 kg」と符号が出る。
  js/lib/calc.js:20 の signedDifference() が体重カードで使われているので、表示を揃える。
  併せて signedDifference() 自身のバグも直すこと:
  signedDifference(69.96, 70) が "0.0" を返し、負であることが消える。
  test/calc.js.test.js にテストを追加すること。

【2-13】週の起点が画面内で食い違う
  js/lib/insights.js:5-9 weekStart() は月曜始まり（「今週のトレーニング」の集計）。
  js/views/home.js:61 のカレンダーは ['日','月',...] で日曜始まり。
  同じホーム画面上で定義が違い、日曜の記録が「今週」に入るか読めない。
  どちらかに統一する。変更した側のテスト（test/insights.test.js / test/calendar.test.js）を更新すること。

【2-14】レップの「＋」が空欄から 2 になる
  js/views/ui.js:49-51:
    const current = field.value === '' ? min : Number(field.value);
  レップは min=1 / step=1 なので stepValue(1, +1, 1, 100) = 2 になる（実測で確認済み）。
  空欄からの初回タップは min（=1）をそのまま採用すべき。
  重量側（min=0, step=5 → 5）の挙動は変えないこと。
  test/input.test.js にテストを追加すること。

【2-15】エラー文言が遠く、原因が伝わらない
  - 「記録する」は画面下、エラーは画面最上部に出る（js/views/ui.js:16）。
  - 重量とレップが両方空でも重量のエラーしか出ず、直して押すと今度はレップのエラーで2往復になる。
  - 文言「0〜500 kgに相当する重量を半角数字で入力してください（表示: kg）」は、
    実際の原因（未入力）を伝えていない。
  受け入れ条件: 未入力と範囲外を区別した文言にし、複数の不備は1回でまとめて伝えること。
  可能なら該当の入力欄の近くに出す。

【2-16】設定の「目標休憩秒数」が既定種目に効かない
  data/exercises.seed.json のシード9種目は全て個別の default_rest_seconds（120/90/150/60）を持ち、
  js/repo.js:144 のグローバル値フォールバックは新規作成種目にしか効かない。
  設定で 90 秒に変えても既定種目の挙動が一切変わらず、壊れているように見える。
  受け入れ条件: この設定が何に効くのかがユーザーに分かること
  （説明文を足す / 種目側の設定への導線を置く / 挙動を変える、のいずれか。
   既存の記録データを壊す変更はしないこと）。

【2-17】刻みチップが種目マスタを恒久的に書き換える
  js/views/session.js:142-145。入力中の 1/2.5/5/10 をタップすると
  repo.saveExercise() が走り、種目マスタの increment_kg / increment_lb が永続的に変わる。
  README にある通りの意図的な仕様だが、一時的な操作に見えるため事故る。
  受け入れ条件: 永続設定が変わることが UI から分かること（挙動自体は変えなくてよい）。

【2-18】部位フィルタチップのタップ領域が狭い
  種目ピッカーの「胸」「肩」「脚」が幅 35px（44px 未満。実測）。44px 以上にする。

【2-19】種目履歴シートの「閉じる」の位置
  js/views/exercise-history.js:14 で「閉じる」がタイトル直後・本文の前にある。
  本文の後ろか、シート上部の一貫した位置へ。

最後に: npm test が全件パスすること。sw.js の CACHE 名を1つ上げ、
js/views/settings.js の「キャッシュ」表示値を同じ値に合わせること。
```

---

## プロンプト3 ── 🟡 保守性（ドキュメント整合・デッドコード・定数一元化・テスト）

```
プロンプト1・2の修正が完了している前提で、以下を行ってください。
このプロンプトでは画面の見た目を変えないこと（変わってしまう場合は報告する）。

【3-1】AGENTS.md を現状に合わせて更新する
AGENTS.md は「仕様と矛盾したら本ファイルを優先」と宣言している最上位ルールだが、
v1 のまま更新されておらず、実装と3箇所矛盾している。
次にこのリポジトリを触る人（または AI）が実装を巻き戻す事故が起きるので、修正する。

  (a) §2「localStorage にトレーニングデータを保存しない（設定値のキャッシュにも使わない）」
      → 実装はテーマのみ localStorage['gym-log-theme'] にキャッシュしている（index.html:14-34）。
        README.md にも記載済み。例外として許可する旨と、その理由（初回描画前にテーマを決めるため）、
        および「テーマ以外には使わない」「読み書きは head 内の同期スクリプト1箇所のみ」を明記する。
  (b) §4「css/style.css 単一ファイル」→ css/glass.css の存在を構成図に反映する。
      §4 のディレクトリ図が js/views/*.js 3ファイル・js/lib/*.js 5ファイルのままなので、
      現状（views 19 / lib 12）に更新する。
  (c) §11「スコープ外」から、実装済みの「推移グラフ」「ダークモード以外のテーマ」を外し、
      v1.1 / v1.2 で採用した経緯が分かるようにする。
実装は一切変えないこと。AGENTS.md（と必要なら README.md）のみの変更。

【3-2】バージョン定数を一元化する
現在4系統あり、うち1つは既にズレている:
  js/db.js:2          SCHEMA_VERSION = 3
  js/repo.js:32       meta.schema_version = 2
  js/lib/transfer.js:57  app_version: '1.1.0'   ← ズレている
  js/views/settings.js:65  バージョン表示 '1.2.0'
  js/views/settings.js:65  キャッシュ表示 'gym-log-glass-3'  ← sw.js:1 の手動コピー
受け入れ条件:
- アプリのバージョン文字列を1箇所（例: js/lib/version.js）に定義し、
  transfer.js と settings.js の両方がそこを読むようにする。値は現行の '1.2.0' に揃える。
- sw.js は ESM ではなくクラシックワーカーなので import できない。
  キャッシュ名の二重管理は、settings.js 側が navigator.serviceWorker / caches から
  実際の値を取得するか、少なくともズレたら分かる形にする。
  難しければ、この項目は「未対応」として理由を報告してよい。
- DB スキーマ版（db.js の 3）と転送形式版（transfer の 2）と meta.schema_version（2）が
  それぞれ何を指すのかを docs/data-model.md に1段落で明記する。
- 既存のバックアップ JSON（app_version '1.1.0' を含むもの）が引き続きインポートできること。
  test/transfer.test.js で往復テストが通ること。

【3-3】sw.js の SHELL 漏れを検知するテストを追加する
sw.js:3 の cache.addAll は全か無かで、SHELL に1ファイルでも欠けがあると install ごと失敗し、
オフラインが静かに壊れる。現在は整合している（SHELL 43件 = 実ファイル42件 + './'）が、
README の手運用に依存している。
受け入れ条件:
- test/shell.test.js を追加し、js/ css/ icons/ と index.html / manifest.webmanifest /
  data/exercises.seed.json の全ファイルが sw.js の SHELL に含まれていること、
  および SHELL に存在しないファイルが書かれていないことを検証する。
- node --test のみで動くこと（追加パッケージ禁止）。sw.js はテキストとして読んでパースしてよい。

【3-4】デッドコードを削除する（実測で参照0件を確認済み）
  (a) css/style.css:58-60 の .settings-link / .settings-row / .setting-name
      → v1.2 の設定画面刷新で不要化。js/ と index.html からの参照 0件。
  (b) css/glass.css の .lg--clear / .lg--tint / .lg--pill / .lg--circle / .lg-interactive
      → 参照 0件（約50行。関連する a11y メディアクエリの記述も含めて整理する）。
  (c) css/glass.css:7-42 の :root / テーマ定義ブロック
      → css/style.css:266 の :root:root:root に全て上書きされており、
        実行時に効いているのは --lg-blur-refract だけ（ブラウザで getComputedStyle 検証済み）。
        実測値:
          --lg-bg    → color-mix(in srgb, #16191B 84%, transparent)   （style.css 側）
          --lg-fg    → #E9ECEC                                        （style.css 側）
          --lg-solid → #1F2427                                        （style.css 側）
          --lg-blur-refract → 3px                                     （glass.css 側。唯一）
        glass.css のテーマブロックを削除し、:root:root:root という詳細度ハックも解消する
        （読み込み順を利用する等）。
  (d) js/views/shell.js:16 が付ける .lg-inner-fill
      → css/style.css:290 の #navigation-dock .tab-button[aria-current]（詳細度 1,2,0）に
        負けており視覚効果が無い。どちらかに寄せる。
削除後、ダーク・ライト両テーマと「透明度オフ」の3状態で見た目が変わっていないことを確認すること。

【3-5】css/style.css の重複した上書きを整理する
300行だが 22KB・最長328文字/行で、同じセレクタが何度も再定義されている:
  .rest              3回（:39 の padding: var(--sp-4) を :82 が padding: 0 で打ち消している）
  #app-content       3回（:148, :261, :281）
  .card / .body-card / .controls / .number-control / .repeat-button  各2回
  @media (prefers-reduced-motion: reduce)  :141 と :252 に完全重複
  :root ブロック      3箇所（:1, :191, :266）
受け入れ条件:
- 同一セレクタの重複定義を1箇所にまとめ、打ち消し合っているプロパティを解消する。
- **見た目を変えない**こと。ダーク/ライト/透明度オフ、ホーム/履歴/設定/セッション/各シートを
  整理前後で比較し、差が無いことを確認する。
- 行数上限（AGENTS.md §6）ではなく「1画面ぶんの見た目を1箇所で読める」ことを目的にする。
  分割が妥当なら分割してよい（その場合 sw.js の SHELL と index.html の link を更新する）。

【3-6】js/views/liquid-glass.js の出所を記録する
237行、他ファイルと明らかに異なるコードスタイルの外部ライブラリのコピー。
AGENTS.md は npm 依存も CDN も禁じているので取り込み自体は妥当だが、
出所・採用バージョン・ライセンスが記録されていないため更新も監査もできない。
docs/ に、どこから取り込んだ何のライブラリか、いつ時点のものかを数行で記録する。
コード自体は変更しないこと。

【3-7】重複ロジックの統合
  js/lib/calc.js:13 estimatedMax() と js/lib/records.js:2 epley() が同じ Epley 式の二重実装
  （丸めの有無だけが違う）。片方に寄せる。
  既存のテスト（test/calc.js.test.js / test/records.test.js）が通ること。

最後に: npm test が全件パスすること。
sw.js の CACHE 名を1つ上げ、js/views/settings.js の「キャッシュ」表示値を同じ値に合わせること。
```

---

## プロンプト4 ── ⚪ パフォーマンス（任意・データが増えてから）

```
2年分（312セッション / 4,992セット）を投入して計測した実測値（デスクトップ Chromium）:
  記録する（1セット）        71ms + 自己ベスト行 35ms   ← 問題なし
  ホーム                     47ms                        ← 問題なし
  種目ピッカーを開く         297ms
  設定タブ                   327ms
  履歴タブ                   346ms（312行を仮想化なしで全描画）
  種目を選んでカードを開く   921ms（初回）               ← 最重要動線
iPhone の IndexedDB はデスクトップより遅いため、実機ではこの数倍で見るべき。

原因（いずれも N+1）:
  js/views/session.js:132  repo.exerciseSets() がその種目の全履歴（約1,250行）を読み、
                           paint() のたび＝1セット記録するたびに走る。
  js/views/history.js:8    recentSessions(Infinity) が sessionSets() をセッション数ぶん発行
                           （1 + 312 トランザクション）。
  js/repo.js:62-66         pickerExercises() が種目ごとに1スキャン。
  js/repo.js:167-169       counts() が3ストア全走査。設定画面を開くたび。

受け入れ条件:
- 「種目を選んでカードを開く」を最優先に改善する（AGENTS.md §1 の最重要動線）。
- 上記と同条件（312セッション / 4,992セット）を投入して、改善前後の数値を報告すること。
  計測せずに「速くなりました」と書かないこと。
- views/ から db.js を直接呼ばない（repo.js 経由）制約は維持すること。
- 読み取りの deleted_at === null フィルタを落とさないこと。
- 既存の計算結果（自己ベスト・推定1RM・総ボリューム・前回比）が変わらないこと。
  npm test が全件パスすること。
```

---

## 補足: レビュー時に見るべき点（あなた＝依頼者向け）

Codex の差分を受け取ったら、最低限これを確認してください。

- `npm test` が通るか
- `grep -rn "innerHTML" js/` が空か
- `grep -rn 'href="/\|src="/' index.html js/ css/` が空か
- **viewport 390x664** で、前回3セット＋今日3セットの種目カードを開いて、
  6行すべてが入力パネルに隠れずに見えるか（プロンプト1 の合否はここだけで決まる）
- ホームを開いた初期表示で開始ボタンが見えるか（iPhone SE 相当 390x568 で）
- ダーク / ライト / 透明度オフ の3状態で崩れがないか
- sw.js の CACHE 名が上がっていて、設定画面の表示と一致しているか
