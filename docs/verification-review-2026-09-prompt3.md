# レビュー2026-09 プロンプト3の確認結果

確認日: 2026-09-20。作業ブランチ: `codex/fix-phase1`。
対象指示は `origin/claude/funny-ptolemy-ccty7b:docs/review-2026-09.md` の共通ブロックとプロンプト3。

## 変更したファイル

| 指摘 | ファイル | 変更内容 |
| --- | --- | --- |
| 3-1 | `AGENTS.md` | テーマキャッシュの限定例外、ガラスCSS、実際のviews/lib構成、採用済みテーマ・推移線を反映。App Shellの静的整合性テストを対象に追加 |
| 3-2 | `js/lib/version.js` | 製品版 `APP_VERSION = '1.2.0'` を一元化 |
| 3-2 | `js/lib/transfer.js` | 書き出しの製品版を共通定数から取得 |
| 3-2・仕上げ | `js/views/settings.js` | 共通製品版とキャッシュ `gym-log-glass-8` を表示 |
| 3-2 | `docs/data-model.md` | DB構造版3・転送形式版2・metaのデータ形式版2の役割を明記 |
| 3-2 | `test/transfer.test.js` | 旧製品版1.1.0のバックアップの読み込み・往復を確認 |
| 3-2・3-3 | `test/shell.test.js` | キャッシュ名の表示不一致、SHELLの不足・不存在・重複を検知 |
| 3-2・仕上げ | `sw.js` | version.jsをSHELLへ追加し、キャッシュを7から8へ更新 |
| 3-4・3-5 | `css/style.css` | 未使用規則を削除。屈折blur 3pxを維持し、詳細度ハック・重複宣言を解消。プロパティを改行して追記の上書きを読まずに値を確認できる形に整理 |
| 3-4 | `css/glass.css` | 未使用バリエーション・テーマ定義を削除。Regularとアクセシビリティ規則を維持 |
| 3-4 | `js/views/shell.js` | 効果のないlg-inner-fill付与を削除 |
| 3-6 | `docs/liquid-glass-source.md` | 取り込み元・日付・ハッシュ・版とライセンスの確認限界を記録 |
| 3-7 | `js/lib/calc.js` | 丸め前のEpley式を共通化。表示は従来どおり小数1桁 |
| 3-7 | `js/lib/records.js` | 共通式を使い、自己ベスト判定の丸め前精度を維持 |
| 3-7 | `test/records.test.js` | 表示値が同じでも微小な推定1RM更新を検出することを確認 |
| 追加依頼 | `js/views/session.js` | 自己ベスト行は重量×レップにし、推定1RMは今日の値・前回比の行に集約 |
| 確認記録 | 本ファイル | 実行結果と判断・未確認事項 |

## 自動確認

- 各項目のコミット前に `npm test` を実行し、全件成功。最終結果は35件成功、失敗0件。
- `grep -rn "innerHTML" js/`: 出力なし（grep終了コード1＝一致なし）。
- `grep -rn 'href="/\|src="/' index.html js/ css/`: 出力なし（一致なし）。
- `git diff --check`: 成功。
- `git diff --exit-code 227ff95 -- package.json js/db.js js/repo.js js/views/liquid-glass.js data/exercises.seed.json`: 成功、変更なし。
- `rg -n 'settings-link|settings-row|setting-name|lg--clear|lg--tint|lg--pill|lg--circle|lg-interactive|lg-inner-fill|:root:root' css js index.html`: 出力なし。
- 配信コードに対するnode_modules・主要CDN URLパターンの検索: 出力なし。
- CSSの完全一致セレクタ数をPythonで集計: `:root` / `.rest` / `#app-content` / `.card` / `.body-card` / `.controls` / `.number-control` / `.repeat-button` は各1箇所。reduced-motionメディアクエリも1箇所。
- SHELLは44件（43ファイル＋`./`）。実ファイル一覧との一致とキャッシュ表示一致は `test/shell.test.js` で成功。
- 旧app_version 1.1.0の往復、schema_version 1の移行、固定シードID、削除状態の保持を既存・追加テストで確認。
- Liquid Glass JSの取り込み元とリポジトリ内ファイルのSHA-256一致を `shasum -a 256` で確認。

## ブラウザー確認

静的サーバーを `python3 -m http.server 8881 --bind 127.0.0.1 --directory /tmp/gym-phase3` で起動。
Codex内ブラウザー、viewport **390×664**、`/gym-log/` と最終コードの `/final/` で確認した。
検証用データとCSS切り替え補助は一時ディレクトリだけに置き、製品に含めていない。

### CSS整理前後

同じDOMで整理前のCSSと整理後のCSSを切り替え、表示要素の矩形と計算済みCSS（疑似要素を含む。SVG内部は除く）を比較。
外観はダーク・ライト・ライト＋透明度オフの3種類。
以下20種類×3外観の**60比較で差0件**。

- ホーム（記録あり）、履歴一覧、設定、種目設定詳細
- セッション空状態、前回3セット＋今日0セット、前回3セット＋今日3セット
- 種目ピッカー、種目新規作成、重量刻み、セット編集、休憩
- 種目履歴、メモ編集、確認ダイアログ、終了サマリー
- 体重入力、過去日付入力、セッション履歴、設定の休憩入力

最後にトークン定義をファイル先頭へ移した状態でも、ホーム・セッションの3外観で差0件を確認。
スクリーンショットでホームと最終セッションを確認した。ピクセル完全一致の画像比較は実施していない。

### 追加の推定1RM表示と連続記録

最終コードで前回値をタップし、スクロールせずに60kg×10回を3セット連続記録。
各回のカード内「推定1RM」は **1行**（`推定1RM 80 kg / 前回比 0.0 kg`）。自己ベスト行は `自己ベスト 70.0 kg × 6`。

| 今日のセット数 | 重量入力の上端–下端 | 記録ボタンの上端–下端 | スクロール位置 |
| --- | --- | --- | --- |
| 0 | 545–593px | 600–656px | 0px |
| 1 | 495–543px | 550–606px | 0px |
| 2 | 495–543px | 550–606px | 0px |
| 3 | 495–543px | 550–606px | 0px |

重量入力は22px、高さ48px。記録ボタンは高さ56px。両方とも全時点で画面内。
今日0セット時の前回3行は183.5–231.5 / 231.5–279.5 / 279.5–327.5pxで、すべて画面内。

## 判断した箇所・意図的な差分

- キャッシュ名の実行時取得は導入せず、指示で許可された最小構成として、表示値とSW定義の不一致をテストで失敗させる方式を選択。
- CSSは配信ファイルを増やさず既存ファイル内で整理した。共有部品・画面の同一セレクタを統合し、値を1プロパティ1行で読めるようにした。
- 製品版定数追加に伴いlibは13ファイルとなるため、構成図も最終状態に合わせた。
- 推定1RMの重複解消は追加依頼で明示された見た目の変更。カードは今日の推定1RM・前回比を残し、過去最高の推定1RMは既存の種目履歴シートで確認する。今日が未記録ならカードに今日の推定1RMを表示しない。
- 3-6の上流バージョン番号・ライセンスは一次情報に記載がなく不明。採用時点とハッシュを記録し、推測で補わなかった。描画JS自体は変更なし。

## 未確認

- Liquid Glassの正式なライセンスと上流バージョン番号（取り込み元に記載なし）。
- iPhone Safari / ホーム画面Web App実機でのガラス描画、safe-area、入力時の自動ズーム、シートのドラッグ・長押し操作。
- 実機でのService Workerの旧キャッシュから8への更新、オフライン再起動、IndexedDBの長期保持。
- これらを確認済みとは扱わない。今回のブラウザー確認はデスクトップ上の狭いviewportであり、iPhone実機の代替ではない。
