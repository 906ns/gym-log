# Liquid Glass の取り込み元

- 対象: `js/views/liquid-glass.js`。ローカルの `liquid-glass-ui` スキルに同梱された `assets/liquid-glass.js`（SVG変位マップによる屈折表現）をコピーしたもの。npmパッケージやCDNは使用していない。
- 取り込み元: `/Users/mia/.agents/skills/liquid-glass-ui/assets/liquid-glass.js`。取り込み日は2026-09-17、コミットは `46563a9`。
- 採用版: バージョン番号・上流リポジトリURLの記載は確認できない。2026-09-20時点で元ファイルとコピーのSHA-256は一致する: `d0c09d07d1408f2dbb92ee5d0eddcacf37fe01d56b96caa49c070dc672fb6ff2`。更新時はこの値と元ファイルを照合する。
- ライセンス: スキル本体、同梱ファイル、参照資料にライセンス表記を確認できない。**不明・未確認**。MIT等のライセンスを推定して付与しない。
- `css/glass.css` も同スキルの同名アセットが取り込み元。2026-09のレビュー3-4で未使用のバリエーションとテーマ定義を削除し、Regular・屈折・アクセシビリティの規則を残した。描画用JSは変更していない。
