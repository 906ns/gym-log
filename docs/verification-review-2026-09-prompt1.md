# 2026-09 レビュー・プロンプト1対応

指定ブランチ `origin/claude/funny-ptolemy-ccty7b` の `docs/review-2026-09.md` にある共通ブロックとプロンプト1を対象とした。作業開始時のブランチ main を維持し、プッシュは行っていない。

## 変更と判断

| 指摘 | ファイル | 変更 |
| --- | --- | --- |
| 1-1 | css/style.css | 入力パネルのbottom stickyを外して通常配置に戻す案Bを採用。前回・今日のセットを覆わない。既存の休憩表示の位置・下端余白・高さ取得は維持 |
| 1-2 | js/views/home.js | overview / start / past / body / calendar の順に変更。過去記録のdisabled条件は変更なし |
| 1-3 | js/views/ui.js | 共通ボタンの再試行前に、そのダイアログまたは現在画面の古いエラーを解除。失敗時は既存catchで再表示。別画面のエラーには触れない |
| 1-4 | js/lib/calc.js, js/lib/units.js | 検証と数値化の前にNFKC正規化。純関数・既存の範囲制限を維持 |
| 1-4 | test/calc.js.test.js, test/units.test.js | 全角数字・小数点、整数条件、範囲外、不正文字のテストを追加。従来の全角拒否期待を更新 |
| 共通 | sw.js, js/views/settings.js | キャッシュ名を双方 gym-log-glass-4 へ更新 |

前回の値を選択する行や記録するボタンの処理、データモデル、固定シードIDは変更していない。入力パネルはスクロールして到達する配置になる。

## 確認結果

- 各項目のコミット前に npm test を実行。1-1〜1-3は26件成功、1-4は28件成功・失敗0。
- `grep -rn "innerHTML" js/`: 出力なし。
- `grep -rn 'href="/\|src="/' index.html js/ css/`: 出力なし。
- `git diff --check`: 成功。
- ローカルの独立した検証用オリジン・データを使用。サブパス配下を静的配信し、Chromiumで確認。古いSWキャッシュと混同しないよう項目ごとに配信パスを分けた。
- 1-1: 390×664、前回3セット（60×10、65×8、70×6とメモ）＋今日3セットを描画。初期状態で前回3行を確認、スクロール後に今日3行を確認。入力パネルはstatic、上端712pxで今日最終行の下端696pxより後。スクロール後は入力パネル下端566.6px、休憩表示上端599pxで重なりなし。休憩高さ取得値65px。
- 1-2: 390×568の初期表示で開始ボタンy270.5〜326.5px、ドック上端496px。進行中では続けるボタンy342.5〜398.5px、ドック上端452px。いずれも完全に表示。過去記録は進行中にdisabled=true。体重・カレンダーはDOMに維持。
- 1-3: 重量abcでエラー表示hidden=false、その後60に修正して保存、hidden=true・文言空・セット数増加を確認。体重未入力と種目名未入力でもエラーを確認し、修正して保存後に記録・種目が追加され、エラー文言が空になることを確認。
- 1-4: 画面に「６２．５」「１０」を入力して保存し、「62.5 kg × 10」のセット行とエラー非表示を確認。kg/lb両方の全角対応とabc/-5/空文字/1.2.3の拒否は単体テストで確認。

## 対象外・未確認

- プロンプト2〜4は今回の依頼対象外。AGENTS.md、package.jsonは編集していない。
- iPhone Safari実機のsafe-area、ソフトウェアキーボード、日本語IMEの確定操作、standalone表示と更新後SW切り替えは未確認。
- 実機で前回・今日の行をスクロールして確認し、入力パネルと休憩表示がキーボード表示時にも重ならないことを確認する必要がある。
