# 日付選択・シートエラーの確認

2026-09-21、`codex/fix-phase1`。

## 変更

- `js/lib/calendar.js`: 今日を引数に14日分の候補を返す純関数。過去トレーニングでは今日を除外し、体重では今日を含める。ミリ秒の引き算ではなく暦日を戻す。
- `js/views/date-control.js`: 日付ボタンから候補一覧を開き、タップで選択。14日ごとに前後へ移動するため、古い記録もキーボード不要。
- `js/views/home.js`: 過去トレーニングと体重測定日の2箇所を置換。保存は既存repo関数のまま。
- `js/views/ui.js`: エラーをシートの取っ手・見出しより下に挿入。詳細画面ではヘッダーの下。
- `test/calendar.test.js`: 今日の除外／包含、候補の連続性、月・年またぎ、うるう年、夏時間付近、不正引数のテスト。
- `AGENTS.md` / `docs/ui.md`: 日付入力の規約と選択手順を更新。
- `sw.js` / `js/views/settings.js`: 新規ファイルをSHELLに追加し、キャッシュ名を `gym-log-glass-9` に統一。

## コマンド結果

- `npm test`: 39件成功、失敗0件。
- `TZ=America/New_York node --test test/calendar.test.js`: 7件成功、失敗0件。夏時間開始・終了付近でも候補が連続する。
- `grep -rn "innerHTML" js/`: 出力なし。
- `grep -rn 'href="/\|src="/' index.html js/ css/`: 出力なし。
- `grep -rn 'type = .date.\|type="date"' js/`: 出力なし。
- `rg -n '\b(document|window|indexedDB|IDBKeyRange)\b' js/lib/`: 出力なし。
- `git diff --check`: 成功。package.json / repo.js / db.jsは変更なし。

## デスクトップで確認した範囲

静的サーバーの `/gym-log/` 配下、Codex内ブラウザー、390×568。検証用の別オリジンで操作した。

1. 過去トレーニングを日付未選択で開始すると「今日より前の日付を選んでください」で拒否。
2. エラー表示時のDOM順は取っ手→見出し→エラー→日付選択→開始ボタン。画面Y座標は取っ手225.5–269.5px、見出し277.5–310.5px、エラー326.5–352px。
3. 過去トレーニングの初期候補は9月20日〜9月7日（確認日の今日9月21日は含まない）。「前の14日」で9月6日〜8月24日に移動。
4. 8月31日をタップして選択表示に反映し、「記録を始める」で8月31日の過去セッションが開いた。
5. 候補行は高さ56px、文字17px。日付を選ぶ操作にテキスト入力は使っていない。
6. 体重シートの測定日初期値は今日9月21日。9月20日をタップして選び保存できた。
7. 同じ9月20日へ再入力すると体重カードが69.0kgに更新され、設定の体重件数は1件のまま。
8. 設定画面のキャッシュ表示は `gym-log-glass-9`。

## 判断と未確認

- 14日より古い入力を失わないようページ送りを付け、年またぎでも区別できるよう候補には年も表示した。
- ネイティブの日付ピッカーに依存しない実装にした。iOSの不具合原因を実機で特定したわけではない。
- **iPhone Safari / ホーム画面Web App実機での修正効果は未確認**。実機で日付候補のタップ、スクロール、ページ送り、選択後の保存を確認する必要がある。
- 実機のsafe-area・ソフトキーボード・旧Service Workerからの更新も未確認。
