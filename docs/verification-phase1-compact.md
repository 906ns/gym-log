# 1-1再追撃: 前回記録と入力の同時表示

## 変更ファイル・判断

- `js/views/session.js`: 入力・休憩をスクロール領域の外の兄弟要素として配置。入力は重量/レップの横並びと記録/刻みの2段。刻み選択はシートへ移動。前回を集計表示より優先し、体調メモはカードの後へ移動。閉じる際は追加領域を削除。再描画時は一覧のスクロール位置を保持。
- `css/style.css`: 入力領域129px、休憩フッター50px（safe-areaが0の検証環境）。入力22px、ボタン44px以上。休憩はposition:staticで領域を確保。カード余白を縮小。
- `docs/ui.md`: 画面構造と小画面用寸法を追記。
- `sw.js` / `js/views/settings.js`: キャッシュをgym-log-glass-6に更新。
- 本文書: 検証結果。

案1と案2を組み合わせた。順序変更だけではなく縦寸法を縮小し、一覧・入力・休憩を別領域にした。データモデル、保存・計算、種目固定IDは変更していない。`--rest-offset` は引き続き不使用。

## 実測（Chromium、390×568、CSS px）

ローカル静的配信の独立オリジンに前回3セット＋今日0セットを用意。最終版で再測定した。

### A: 開いた瞬間

カード先頭を表示するためscrollTop=8。手動スクロールなし。

| 前回行 | 上端〜下端 | 高さ |
| --- | --- | --- |
| 1 | 175.5〜223.5 | 48 |
| 2 | 223.5〜271.5 | 48 |
| 3（メモありの行を含む） | 271.5〜319.5 | 48 |

前回3行すべてがスクロール領域48〜439内に完全表示。前回行をタップして重量70・レップ6へ転記できることも確認。

### B: スクロール操作なしで連続3セット

座標クリックで記録し、ツールによるボタンへの自動スクロールに依存しない方法で測定。

| 今日のセット数 | 重量入力欄 | 記録ボタン | 一覧scrollTop |
| --- | --- | --- | --- |
| 0 | 449〜497 | 504〜560 | 8 |
| 1 | 399〜447 | 454〜510 | 8 |
| 2 | 399〜447 | 454〜510 | 8 |
| 3 | 399〜447 | 454〜510 | 8 |

最初の記録で休憩領域50pxが表示される分だけ上がる。その後は同じ位置。全回、重量と記録ボタンは画面内に完全表示され、手動スクロールは0回。

### C: 一覧に重ならない

記録後の領域境界はスクロール領域48〜389、入力領域389〜518、休憩領域518〜568。スクロール領域はoverflow-y:autoで境界外を描画しないため、入力・休憩との描画の重なりは0px。

3セット記録後も前回3行は175.5〜319.5のまま。今日の3行は一覧をスクロール（scrollTop=227）すると127.5〜181.02、181.02〜234.55、234.55〜288.07に完全表示され、入力・休憩との重なりは各0px。前回と今日の全6行を同時表示する仕様ではなく、必要な一覧を独立した領域内でスクロールする。

### D: サイズ

- 重量/レップ入力: 22px、入力領域69×48px。
- 増減ボタン: 52×48px。
- 刻みボタン: 88×56px。
- 記録ボタン: 278×56px。
- 前回行: 356×48px。
- 刻み変更シートを開き、2.5へ変更して閉じ、ボタン表示へ反映することを確認。

## コマンド確認

- npm test: 28件成功、失敗0。
- node --check js/views/session.js: 成功。
- grep -rn "innerHTML" js/: 出力なし。
- grep -rn 'href="/\|src="/' index.html js/ css/: 出力なし。
- rg 'rest-offset' js/ css/: 出力なし。
- git diff --check: 成功。

## 未確認

実機iPhone Safari、safe-areaの実寸、ソフトウェアキーボード表示中、standalone起動とSW更新は未確認。非常に長い種目名・メモの場合、折り返し行数に応じて一覧のスクロールが必要になる。上記実測は標準種目名と短いメモ付き前回3セット。
